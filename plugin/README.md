# option-desk plugin

Built by `scripts/package.py` from `shell/skills`, `.claude/commands` and `.claude/agents`. Do not edit here: edit the sources and rebuild, or the next build will discard your changes.

The MCP server entry expects `optiondesk-mcp` on PATH, which `install.sh` puts there. The skills, commands and agents work without it; only the tools need it.
